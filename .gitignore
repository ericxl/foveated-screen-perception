# OS
.DS_Store

# LaTeX build artifacts
*.aux
*.bbl
*.blg
*.log
*.out
*.toc
*.fls
*.fdb_latexmk
*.synctex.gz

# docs/ contains rendered outputs (main.pdf, index.html).
# These are tracked so GitHub Pages can serve them from the /docs source.
